class Checker(int x, int y){
    int max() if x>=y then { x } else {y};
    int min() if x<y then { x } else {y};
};
let 
    int x=42; 
    int y=41; 
    Checker c = new Checker(x,y); 
in 
    print(c.max());