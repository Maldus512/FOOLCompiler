class block(int x, int y, int z) {
    void main() print(x);
}
class bblock implements block(int k) {
    void main() print(k);
};
let
    block foo() 
        let
            bblock b = new bblock(1,2,3,4);
            block b1= new block(1,2,3);
        in
            b1;
    block b = new block(4,5,6);
in
    foo();
